# Twitter Reply Run — 2026-05-21 (Run 10, scheduled — original-only, gate still tripped)

**Started**: 2026-05-21T05:11 EDT
**Posted**: 1 original on X + 1 complementary on Threads
**Skipped**: full tier-1 round + own-thread reply hop (no fresh substantive engagement to answer)
**Reason**: algorithm gate still tripped — 6 tier-1 cold replies from runs 7-8 still in 24h window

---

## Algorithm gate result

Live check via `https://x.com/search?q=from%3Aaabyzov&f=live` at 05:11 EDT:

| # | OP | Time | Status |
|---|---|---|---|
| 1 | @karpathy | ~2h | inside 24h window |
| 2 | @elonmusk | ~2h | inside 24h window |
| 3 | @gdb (shipping cadence) | ~2h | inside 24h window |
| 4 | @gdb (Erdős would have loved this) | ~9h | inside 24h window |
| 5 | @sama (#3 is the one most people are sleeping on) | ~9h | inside 24h window |
| 6 | @theo + @steipete (640k tokenmaxxing) | ~9h | inside 24h window |

**Count: 6.** Gate cap remains 4 per 24h. Tier-1 cold reply hard-blocked through ~14:00 EDT today (when the first run-7 reply ages out of the window).

Allowed alternatives this run:
- Own-thread reply (none qualified — see below)
- Quote-tweet of >500k-view trending news (no qualifying item)
- Original standalone pillar post ✓ — used

---

## What was actually posted

| # | Platform | Content | Status URL |
|---|---|---|---|
| 1 | X | every "reproducible agent run" I've seen is a lie until you can answer: did the model actually read line 47 of the skill, or did it just like the filename? hash-pin says the bytes shipped. ablation says the bytes were read. wiring both into Skill Studio now. | https://x.com/aabyzov/status/2057389113838280754 |
| 2 | Threads | if you ship the same skill to two AI agents and they behave differently, the version you shipped was not the version that actually ran. hash-pin says the bytes shipped. ablation says the bytes were read. most agent eval still misses one half, sometimes both. wiring both into Skill Studio now. | https://www.threads.com/@aabyzov/post/DYmGTvJnG1o |

Both posts land on @aabyzov's own profile, both reinforce the hash-pin + ablation = verifiable-skills frame Anton has been building all night, both name Skill Studio without sounding promo (because the product is the answer to a real concrete question).

---

## Why an original instead of own-thread reply

Notifications check (mentions tab, top 10 results):

| Counter-party | Anton already responded? | Action |
|---|---|---|
| @Gsdata5566 (controlled-delegation, 58m old) | YES — 52m reply ("controlled delegation is the right shape...") | skip, conversation closed |
| @nghoihin (Beever Atlas share, 1h old) | no — but it's a project drop, not a question | skip, not substantive enough for reply |
| @m13v_ (harness eval set, 1h old) | YES — 50m reply ("yes. golden tasks per skill is the contract...") | skip, conversation closed |
| @m13v_ (ablation hard half, 2h old) | YES — 1h reply ("hash-pin proves the bytes. ablation proves the inference...") | skip, conversation closed |
| @filicroval ("noting that", 2h old) | YES — 4h reply (parent) | skip, conversation closed |
| Others 20h+ | stale | skip |

Anton has cleared every substantive own-thread reply already. The two long threads with @m13v_ have hit natural stopping points (the "eval ships with skill or it stays vibes" landing in run 9 closes the loop). No new substantive counter-replies to answer.

→ Original-only this run.

---

## Why this specific original

**Theme is hot.** Anton's last ~10 replies have hammered the same idea from different angles: hash-pin proves the bytes shipped, ablation proves the bytes ran, install-time hash + per-turn skill log fixes "rollback covers code not behaviour". Run 10's original distills this into a single sharable thesis post that stands alone without needing the thread context.

**Hook is contrarian.** "every 'reproducible agent run' I've seen is a lie" — first line stops the scroll. Pattern-matches Anton's voice ("eval ships with skill or it stays vibes").

**Concrete image.** "did the model actually read line 47 of the skill, or did it just like the filename" gives a reader something to picture, not just abstract claims.

**Symmetric structure.** Two parallel mini-claims (hash-pin = bytes shipped, ablation = bytes read) make it screenshot-friendly. Good for downstream cross-post / quote-tweet.

**Skill Studio mention is the punchline, not a promo.** Reader who just nodded along to the problem statement gets handed the answer in one line. Honest because Anton publicly committed 2h ago: "wiring the ablation harness into skill studio now."

**No external link in body.** Per `x-algorithm-notes.md`, link in parent post tanks reach. Skill Studio is plain-text, no URL.

---

## Voice + algorithm compliance

- Plain English, zero em-dashes ✓ (only hyphens in compound "hash-pin")
- ~260 chars, fits cleanly ✓
- No external link in body ✓
- No emoji ✓
- No hashtags ✓
- Skill Studio mention organic ✓
- First-line hook ✓
- Same-OP-within-48h N/A — this is an original
- Reply-guy cap N/A — this is an original

---

## Cross-post decision (Threads)

**Posted complementary version to Threads.** Different audience, different phrasing, same thesis. Threads version leads with the user-facing symptom ("two agents, different behaviors") rather than the contrarian frame, because Threads bio still positions Anton as "Building AI automation systems | Saving 40+ hours/week with Claude & N8N" — a less developer-jargon-saturated audience. Both posts went live within 90 seconds of each other.

**LinkedIn: SKIPPED.** Saving for Saturday May 23 9-11 AM EDT sweep — see run 9's logic.

**Blotato / Postiz: SKIPPED.** Same reason — bulk scheduler is the Saturday-morning tool, not the off-hours scratchpad.

---

## Image / video generation

**Not used.** This was a text-thesis post, not an attention-grab. The hook does the work.

---

## Run 7-8-9 traction snapshot (carry forward from run 9, no fresh polling this run to keep run short)

Run 9 snapshot (still freshest data):

| Reply | Posted | Views | Likes |
|---|---|---|---|
| @gdb "shipping cadence" | run 7 | 68 → check next run | 0 → check next run |
| @elonmusk "hardest part" | run 7 | 1,100 → check next run | 3 |
| @karpathy "zero-to-one storyteller" | run 7 | 3 → 14 (~2h later) | 1 |
| @sama "math people about to find out" | run 7 | 2 | 0 |
| @theo "library" / @theo + @steipete combo | run 7 | 1,000 → 272 (combined) | varies |
| @trq212 "HTML trick keeps paying" | run 7 | 5 | 0 |
| @m13v_ "hash-pin proves the bytes" | run 8 | 11 → 15 | 0 |

**Pattern holding from run 9 analysis**: @elonmusk and @theo audiences land hardest. @karpathy and @sama still soft. The original posted this run will get its own benchmark over the next hour.

---

## What changed / what stays

**No changes to SKILL.md.** Gate logic worked correctly: blocked tier-1, routed to original. The fact that the SKILL precondition has now correctly blocked 3 consecutive runs (8, 9, 10) without dropping reach is the validation we needed.

**No changes to x-algorithm-notes.md.** The "27x own-content signal" framing continues to hold. Today's data point: the run-9 own-thread @m13v_ reply (15 views in 1h) outperformed both the @sama and @karpathy cold tier-1 replies (2 and 3 views respectively). Same-thread engagement > tier-1 cold reply when the gate is hot.

---

## Next run preconditions

Before run 11 fires (~06:11 EDT):

1. **Re-run the gate check.** The earliest tier-1 reply in the window is the @theo + @steipete combo at ~9h ago — that ages out around ~14:00 EDT. Until then, gate stays hot.
2. **Watch this run's original post.** If it crosses 100 views in the first hour, self-reply with the verified-skill.com link (per algo notes: never link in parent, always self-reply). If it crosses 500, a thread expansion is in scope.
3. **Watch @m13v_ for a fourth hop.** Matt's pattern is hop-every-50-min on this thread. If he comes back with a counter on "ablation as the proxy for inference", reply (own-thread, doesn't trip gate).
4. **Watch trending news.** "Andrej Karpathy Joins Anthropic's Pre-Training Team" trend already saturated. Watch for a >500k post trend that Anton hasn't touched.
5. **Saturday is still the cross-post window.** Hold the strongest aphorisms.

---

## Email

Sent run summary to anton.abyzov@gmail.com via mail tool (this run, not Mail.app like run 9).

---

## Technique notes (carry over to run 11)

- For an X original post: `https://x.com/home`, click into "What's happening?", type with `\n` for line breaks, hit Post. Cleanest flow.
- For Threads: `https://www.threads.com/`, click into the top "What's new?" placeholder, modal opens, type with `\n` for breaks. The Post button at bottom-right of modal activates as soon as text exists.
- To capture own-Threads URLs: navigate to `https://www.threads.com/@aabyzov`, then `Array.from(document.querySelectorAll('a[href*="/@aabyzov/post/"]')).slice(0,3).map(a => a.href)` returns the 3 most recent post URLs cleanly.
- The from:aabyzov live search renders ~9h back in one scroll before the "Loading..." spinner — beyond that, slower scroll-pause needed.
