# C4 Context Diagram

*Auto-generated by Intelligent Analyzer*

```mermaid
C4Context
    title System Context Diagram - SpecWeave

    Person(developer, "Developer", "Uses CLI to manage increments and documentation")
    Person(pm, "Product Manager", "Reviews specs and tracks progress")

    System(specweave, "SpecWeave", "Spec-Driven Development Framework")

    System_Ext(github, "GitHub", "Issue tracking and repository hosting")
    System_Ext(jira, "Jira", "Project management and issue tracking")
    System_Ext(ado, "Azure DevOps", "Work item management and repos")
    System_Ext(docusaurus, "Docusaurus", "Documentation site generator")

    Rel(developer, specweave, "Creates increments, runs commands")
    Rel(pm, specweave, "Reviews progress, validates specs")
    Rel(specweave, github, "Syncs issues, imports work items")
    Rel(specweave, jira, "Syncs epics/stories, imports items")
    Rel(specweave, ado, "Syncs work items, imports data")
    Rel(specweave, docusaurus, "Generates MDX documentation")
```