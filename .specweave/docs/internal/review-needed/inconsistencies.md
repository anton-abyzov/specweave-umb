# Inconsistencies

*Auto-generated by Intelligent Analyzer*

## ICS-001: Multiple api patterns in use

Found 8 different api patterns: REST API Integration with Axios (core); REST API Client with Pagination and Rate Limiting (cli); REST API Integration (GitHub, JIRA, ADO) (sync, importers); REST API Client with Basic Authentication (integrations); Atlassian Document Format (ADF) for Rich Content (integrations); Rate Limit Detection and Handling (integrations); REST API Client using Octokit (metrics); Rate Limiting with Exponential Backoff (metrics, importers)

**Suggested action**: Standardize on a single api pattern

## ICS-002: Multiple integration patterns in use

Found 18 different integration patterns: Multi-Provider LLM Abstraction (core); Multi-Platform Git Provider Abstraction (core); Internationalization (i18n) (cli); DORA Metrics Calculation (cli); Bidirectional Sync with External Tools (cli); Token cost estimation (utils); External service validation (utils); Multi-Repository Selection (init); Plugin Compilation System (adapters); Internationalization (i18n) with System Prompts (adapters); Markdown Documentation Generation (adapters); LLM-native translation system using Claude Haiku (locales); Closure Metrics and Monitoring (sync); Bidirectional Sync with Conflict Detection (integrations); Report Generation with Markdown (metrics); Hook Protocol for Claude Code (hooks); External tool integration abstraction (living-docs); Multi-Platform Integration (GitHub, JIRA, ADO) (types)

**Suggested action**: Standardize on a single integration pattern

## ICS-003: Multiple structure patterns in use

Found 35 different structure patterns: YAML/JSON Configuration Management (core); TypeScript Strict Mode with ESM (core); Commander.js CLI Framework (core); Internationalization (i18n) (core); Living Documentation Hierarchy (core); Configuration Management with JSON Schema Validation (cli); Interactive CLI Wizard (cli); Builder pattern for documentation (utils); Barrel exports for module organization (utils); ESM module helpers (utils); Handlebars templating with custom helpers (templates); PRD/HLD/ADR/Spec/Runbook documentation framework (templates); Increment-based task management with US-AC traceability (templates); Report Generator Pattern (init); Registry Pattern with YAML Configuration (adapters); Package Root Detection (adapters); JSON-based i18n with nested key structure (locales); Interpolation support with dual syntax (locales); RFC Document Generation (integrations); TypeScript Strong Typing with Interfaces (metrics); Utility Module Pattern (metrics); ESM Module System (metrics); CLI Entry Point Pattern (metrics); Platform Detection Caching (hooks); Glob-based file discovery (living-docs); Docusaurus-compatible output generation (living-docs); Priority-Based Work Management (types); TypeScript Type Discrimination (id-generators); Regex-based Parsing (id-generators); Defensive Programming with Validation (id-generators); Zero-padded Numeric Formatting (id-generators); ASCII Progress Bar Visualization (progress); Linear ETA Extrapolation (progress); TypeScript Interface-First Design (progress); Convention over Configuration (testing)

**Suggested action**: Standardize on a single structure pattern

## ICS-004: Multiple architecture patterns in use

Found 80 different architecture patterns: Singleton Registry Pattern (core); Plugin Architecture (core, cli); Background Job Processing (core); Event-Driven Hook System (core); State Machine for Increment Lifecycle (core); Circuit Breaker for Sync Operations (core); Discrepancy Detection (Code-to-Spec) (core); Adapter Pattern for Multi-Platform Support (cli); Background Job Worker Pattern (cli); Session Registry for Process Management (cli); Increment Lifecycle State Machine (cli); Intelligent Codebase Analysis Pipeline (cli); Hook System for Event-Driven Actions (cli); Dependency Injection via Logger interface (utils); Progressive disclosure pattern (utils); Strategy pattern for project resolution (utils); Configuration-driven structure detection (utils); C4 Model architecture diagrams with Mermaid (templates); Template caching for performance optimization (templates); Rule-Based Classification without LLM (init); Strategy Pattern for Team Recommendations (init); Factory Pattern for Question Generation (init); Domain-Driven Design with Value Objects (init); Weighted Scoring Algorithm (init); Configuration Object Pattern (init); Adaptive Questioning System (init); Adapter Pattern for Multi-Tool Support (adapters); Template Method Pattern (adapters); Strategy Pattern for Tool Detection (adapters); Singleton pattern for locale manager (locales); Content preservation placeholders (locales); Fallback locale strategy (locales); Wrapper/Decorator Pattern for Permission Enforcement (sync); 3-Layer Idempotency Strategy (sync); Status State Machine Mapping (sync); Dependency Injection for Logger (sync, validators, progress); ConfigManager for Non-Secret Configuration (sync); Multi-Project Support with Profile Resolution (sync); Three-Tier Lazy Loading with Caching (integrations); Hierarchical Data Mapping (integrations); Progress Tracking with Cancelation Support (integrations); Dependency Injection for Logging (integrations, testing); Work Item Type Polymorphism (integrations); Strategy Pattern for Metric Calculation (metrics); Benchmark Classification System (metrics); Adapter Pattern for Multi-Platform Abstraction (importers); Async Generator Pagination (importers); Strategy Pattern for Hierarchy Mapping (importers); Factory Pattern for Importer Creation (importers); Coordinator/Orchestrator Pattern (importers); Callback-based Progress Reporting (importers); Type Normalization for Cross-Platform Consistency (importers); Dependency Injection via Constructor Options (importers); Event-Driven Architecture with Event Queue (hooks); Background Daemon Process Pattern (hooks); Handler Routing with Timeout (hooks); Scheduled Job Orchestration (hooks); Repository pattern for ID management (living-docs); Multi-tenant directory structure (living-docs); Chronological insertion with gap-filling algorithm (living-docs); Keyword-based theme classification (living-docs); TypeScript Interface-First Design (types); Factory Functions for Default Values (types); Event-Driven Session Management (types); AI Model Cost Optimization Strategy (types); Discriminated Union Types (types); Repository Pattern for Configuration (config); Environment Variable Override Pattern (config); Default Configuration Pattern (config); Regex-based Document Parsing (generators); TypeScript Interface-driven Design (generators); Line-by-line State Machine Parsing (generators); Traceability Matrix Pattern (generators, validators); Factory Pattern for ID Generation (id-generators); Sequential ID with Gap-aware Generation (id-generators); Validator Pattern with Detailed Results (validators); Parser Integration Pattern (validators); Internal/External Item Distinction (validators); Observer/Polling Pattern for Cancelation (progress); Code Generation from DSL (testing)

**Suggested action**: Standardize on a single architecture pattern

## ICS-005: Multiple data patterns in use

Found 42 different data patterns: Repository Pattern for External Tools (core); Zod Schema Validation (core, init, config); File-based Locking for Concurrency (cli); Atomic file operations with file locking (utils); YAML frontmatter parsing (utils); Repository pattern for session management (utils); Caching with TTL (init); Static Configuration Database (init); YAML Frontmatter Parsing (adapters); Unicode-based language detection heuristics (locales); YAML Frontmatter as Metadata Store (sync); Conflict Resolution with Timestamps (sync); Repository Pattern for Sync State (sync); Duplicate Detection and Prevention (sync); Repository Pattern for Data Access (integrations); YAML Frontmatter for Document Metadata (integrations); TTL-Based Cache with Stale Fallback (integrations); Filter Presets with Custom JQL Support (integrations); Statistical Analysis with Percentiles (metrics); Repository Pattern for Duplicate Detection (importers); Gray-matter Frontmatter Parsing (importers); Caching with Cache Invalidation (importers); File System as Data Store (importers); Atlassian Document Format (ADF) Parsing (importers); Acceptance Criteria Extraction via Regex (importers); Sync Metadata Tracking (importers); File-Based Locking (Atomic mkdir) (hooks); Atomic File Operations (hooks); File-based locking for atomic operations (living-docs); YAML frontmatter parsing with gray-matter (living-docs); Collision detection with variant checking (living-docs); Versioned Schema for Data Migration (types); Cache Invalidation via mtime Tracking (types); ISO-8601 Timestamp Standard (types); Atomic Read-Modify-Write (config); TypeScript Native fs/promises (config); YAML Frontmatter Parsing with js-yaml (generators); File System I/O with Node.js fs (generators); JSON Export for Reports (validators); State Persistence with TTL (progress); File-based Error Logging (progress); Map-based Task Grouping (progress)

**Suggested action**: Standardize on a single data pattern

## ICS-006: Multiple cloud patterns in use

Found 5 different cloud patterns: Handlebars Template Engine for IaC (core); Serverless Platform Intelligence (core); Infrastructure as Code generation for multi-cloud (AWS Lambda, Azure Functions, GCP Cloud Functions, Firebase, Supabase) (templates); Kubernetes manifests and Helm chart templates (templates); Serverless Cost Optimization Analysis (init)

**Suggested action**: Standardize on a single cloud pattern

## ICS-007: Multiple security patterns in use

Found 7 different security patterns: Credential Management with Environment Variables (core); Credential Management Pattern (cli); Compliance-Driven Architecture Recommendations (init); Audit Logging via Interceptor (sync); Project Root Validation Guard (sync); MDX/HTML Sanitization (importers); Whitelist/Blacklist Field Filtering (validators)

**Suggested action**: Standardize on a single security pattern

## ICS-008: Multiple testing patterns in use

Found 14 different testing patterns: Vitest Unit Testing Framework (core); Playwright E2E Testing (core); Unit Testing with Vitest (cli); E2E Testing with Playwright (cli); Vitest unit testing framework with AAA pattern (templates); Playwright E2E testing templates (templates); Code quality standards generation from codebase analysis (templates); Health scoring with weighted metrics (living-docs); Acceptance criteria parsing and tracking (living-docs); Validation with Descriptive Error Messages (generators); Coverage Metrics Calculation (validators); Test Specification Pattern (testing); Integration Testing Framework (testing); Test Report Generation (testing)

**Suggested action**: Standardize on a single testing pattern

## ICS-009: Multiple deployment patterns in use

Found 5 different deployment patterns: Terraform HCL generation with environment-specific tfvars (templates); SRE incident response templates (runbooks, post-mortems) (templates); Environment-specific deployment configurations (templates); Cross-Platform Command Detection (adapters); Cross-Platform Process Spawning (hooks)

**Suggested action**: Standardize on a single deployment pattern

## ICS-010: Multiple frontend patterns in use

Found 2 different frontend patterns: React component and hook templates (templates); Interactive CLI with Inquirer (init)

**Suggested action**: Standardize on a single frontend pattern

## ICS-011: Multiple auth patterns in use

Found 2 different auth patterns: Multi-Tenant Credential Management (integrations); Environment Variable Configuration with Normalization (integrations)

**Suggested action**: Standardize on a single auth pattern
