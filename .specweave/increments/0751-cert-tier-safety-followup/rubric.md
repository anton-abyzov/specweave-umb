---
increment: 0751-cert-tier-safety-followup
title: "Cert-tier safety follow-up: bulk Skill backfill + bug fixes + invariant test"
generated: "2026-04-26"
source: closure-pipeline
version: "1.0"
status: closure-blocked
---

# Quality Rubric: 0751-cert-tier-safety-followup

> Generated by sw-closer at closure attempt on 2026-04-26.
> Verdict: **CLOSURE BLOCKED** — 2 high grill findings + 2 medium remain.

## Functional Criteria

- [x] **PASS — AC-US1-01..04**: Bulk endpoint exists at `src/app/api/v1/admin/recompute-skill-cert-tiers/route.ts`, uses single `db.skill.updateMany()`, returns `{ ok, updated, sample }`, idempotent, auth-gated. Tests: route.test.ts vendor-org bulk update + auth gating describe blocks. [blocking]

- [!] **FAIL — AC-US1-05**: Prod deploy + backfill run **not executed**. `logs/backfill-skill-2026-04-26.md` does not exist. Task T-015 marked complete without the deliverable. ~151 stale prod rows likely still display "Security-Scanned" instead of "Trusted Publisher". [blocking]

- [x] **PASS — AC-US2-01, AC-US2-02, AC-US2-04**: Unblock route no longer hardcodes `certTier: "VERIFIED"`; vendor-org skills receive `CERTIFIED + VENDOR_AUTO`; search-shard upsert carries derived `certTier`. Verified by tests + architecture invariant. [blocking]

- [~] **PARTIAL — AC-US2-03**: Non-vendor unblock returns `VERIFIED + AUTOMATED_SCAN` when `skill.certMethod` is undefined (test asserts this). However the F-002 mitigation that **preserves** an existing `skill.certMethod` (e.g. `LLM_JUDGE`) is **untested** — `makeSkill()` helper omits `certMethod`, so the preservation branch falls through to the default. AC text also drifts from implementation. [blocking]

- [x] **PASS — AC-US2-05**: `rebuild-index/route.ts` KV-recovery path now calls `deriveCertTier()` in the upsert.create payload. 3 tests assert vendor / non-vendor / community paths. [blocking]

- [x] **PASS — AC-US3-01**: Per-row structured `console.log` emits `event: "recompute-version-cert-tier"` with full before/after payload. Test asserts payload shape. [blocking]

- [x] **PASS — AC-US3-02**: Summary log emitted both on populated runs (line 122) and empty runs (line 51, F-003 fix). Operators tailing CF logs can confirm endpoint executed regardless of work. [blocking]

- [x] **PASS — AC-US3-03**: `safeLog` helper wraps `console.log` in try/catch. Test "survives console.log throwing" asserts batch completes. [blocking]

- [x] **PASS — AC-US4-01, AC-US4-03, AC-US4-04**: `cert-tier-derivation.test.ts` exists, walks `src/`, allowlist matches AC text, test passes against current codebase. [blocking]

- [~] **PARTIAL — AC-US4-02, AC-US4-05**: Regex itself is smoke-tested with literal strings; allowlist guard test confirms files exist. But the end-to-end pipeline (walk → isAllowed → regex → error message) is **never exercised against a real fixture file**. T-011 test plan called for this; deliverable not produced. A bug in `isAllowed()` (off-by-one suffix match) could silently disable detection. [advisory]

- [x] **PASS — AC-US5-01..03**: TDD cycle followed (RED → GREEN), 22/22 ACs have test coverage, 17/17 increment-specific tests pass, no NEW failures introduced (baseline ~144 pre-existing kept). [blocking]

## Code-Review Criteria

- [x] **PASS — code-review-report.json**: Sequential 8-perspective review. 5 MEDIUM findings (F-001..F-005) addressed with targeted fixes. 0 critical / 0 high / 0 medium remaining. Report at `reports/code-review-report.json`. [blocking]
  - Evaluator: sw:code-reviewer

## Grill Criteria

- [!] **FAIL — grill-report.json (re-grilled 2026-04-26)**: 2 high findings (G-001 prod-backfill missing, G-002 untested certMethod preservation branch) + 2 medium (G-004 architecture test e2e fixture, G-006 stale comment). Ship readiness: NEEDS REVIEW. Verdict: FAIL. [blocking]
  - Evaluator: sw:grill

## Judge-LLM Criteria

- [ ] **PENDING — judge-llm-report.json**: Not produced — closure halted at grill gate before judge-llm step. [blocking]
  - Evaluator: sw:judge-llm

## Test Coverage

- [x] **PASS — Unit/integration tests pass**: 17/17 increment-specific tests pass. 9 unrelated pre-existing JWT-signing failures (baseline from 0744). [blocking]

- [!] **FAIL — Prod verification**: AC-US1-05's manual smoke (`GET /api/v1/skills/anton-abyzov/specweave/pm` returns `certTier: 'CERTIFIED'`) cannot be performed because deploy + backfill never ran. [blocking]

## Documentation

- [x] **PASS — spec/plan/tasks**: All present and consistent. [blocking]
- [~] **PARTIAL — task accuracy**: T-015 marked complete without prod deploy. Should be unchecked or completed for honest accounting. [advisory]

---

## Closure Summary

**Status: NOT CLOSED.** The closure pipeline halted at the grill gate.

**To unblock closure, address in order:**

1. **G-001 (high)** — Either: (a) run `npm run deploy` from `repositories/anton-abyzov/vskill-platform/`, then `POST /api/v1/admin/recompute-skill-cert-tiers` with valid auth, capture the response to `logs/backfill-skill-2026-04-26.md`, force-add via `git add -f`. OR: (b) un-check AC-US1-05 in spec.md and T-015 in tasks.md, document prod backfill as pending in a follow-up increment.

2. **G-002 (high)** — Add one test in `cert-tier-derivation.test.ts` that seeds `makeSkill({ certMethod: 'LLM_JUDGE' })` for a non-vendor skill and asserts post-unblock `certMethod === 'LLM_JUDGE'`. Five lines, exercises the F-002 preservation branch.

3. **G-004 (medium)** — Add fixture-based test in `cert-tier-derivation.test.ts`: `beforeAll` writes `src/__tests__/_fixture-cert-tier-violator.ts`, asserts violation detected with file path in error, `afterAll` unlinks.

4. **G-006 (medium)** — Reword comment at `rebuild-index/route.ts:122-125` to: "0751 AC-US2-05: derive certTier from vendor status on the CREATE path. The UPDATE path intentionally does not touch certTier — use recompute-skill-cert-tiers to backfill stale existing rows."

After fixes, re-run `sw:code-reviewer --increment 0751` → `sw:grill 0751` → `sw:judge-llm 0751` → `specweave complete 0751-cert-tier-safety-followup --yes`.
